import { Layout } from "../../components/bloglayout";

export const meta = {
  title: "[draft] dont read me",
  date: "Feb 17 2022",
  subtitle: "",
  draft: true,
};

# {meta.title}

> this page serves as an example draft for now

export default ({ children }) => <Layout>{children}</Layout>;
