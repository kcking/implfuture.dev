import { MDXProvider } from "@mdx-js/react";
import { Layout } from "../../components/bloglayout";
export default ({ children }) => <Layout>{children}</Layout>;

import { BevyExample } from "../../components/bevy_dynamic";

export const meta = {
  title: "Intro to Bevy",
  date: "Feb 19 2022",
  draft: true,
  subtitle: "yea",
};

# {meta.title}

> {meta.subtitle}

<canvas id="bevy-canvas" className="w-full max-w-1/2 sm:w-1/4"></canvas>

{<BevyExample height={480} width={600}/>}
